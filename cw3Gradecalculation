let score=76
function GradeCalculation(){
    switch (score) {
        case (score>='90'):
            return 'Grade A'
        
        case (score>='75'):
            return 'Grade B'
    
        case (score>='55'):
        return 'Grade C'

        default :
        return 'Fail'
        
    }
}
let result=GradeCalculation(score)
console.log(result);

